{#
/**
 * @file
 * Default theme implementation for page titles.
 *
 * Available variables:
 * - title_attributes: HTML attributes for the page title element.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title: The page title, for use in the actual content.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 *
 * @ingroup templates
 *
 * @see template_preprocess_page_title()
 */
#} 

    {# {% if content.field_banner_image.entity.field_media_image.entity.uri.value %}
    <div class="banner-wrapper 1" style="background-image: url('{{ file_url(content.field_banner_image.entity.field_media_image.entity.uri.value) }}');"> 
    {% elseif field_banner_image %}
    <div class="banner-wrapper 2" style="background-image: url('{{ banner_image }}');"> 
    {% else %}
    <div class="banner-wrapper 3" style="background-image: url('/themes/custom/vermontgold/images/Banner1.jpg');">
    {% endif %} #}
    {% if title %}
    <div class="page-header-wrapper">
        <h1{{ title_attributes.addClass('page-header') }}>{{ title }}</h1>

        {% if content.getCreatedTime %}
            <div class="info">
                <span class="date">{{ content.getCreatedTime|format_date('custom','F j, Y') }} {{ date }}</span>
                {# {% if field_categories %} #}
                    <span> {{content.field_categories}} </span>
                {# {% endif %} #}
            </div>
        {% endif %}
    </div>
    {% endif %}