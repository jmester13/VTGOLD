{#
/**
 * @file
 * Default theme implementation for page titles.
 *
 * Available variables:
 * - title_attributes: HTML attributes for the page title element.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title: The page title, for use in the actual content.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 *
 * @ingroup templates
 *
 * @see template_preprocess_page_title()
 */
#} 

{% if node.field_media_image.entity.field_media_image.entity.uri.value %}
<div class="banner-wrapper 1" style="background-image: url('{{ node.field_media_image.entity.field_media_image.entity.uri.value}}');"> 
{% elseif node.field_media_image.entity.field_media_image.entity.uri.value  %}
<div class="banner-wrapper 2" style="background-image: url('{{ field_media_image.entity.uri.value  }}');"> 
{% else %}
<div class="banner-wrapper 3" style="background-image: url('/themes/custom/vermontgold/images/Banner1.jpg');">
{% endif %}

  {% if title %}
    <h1{{ title_attributes.addClass('page-header') }}>{{ title }}</h1>
  {% endif %}
  </div>